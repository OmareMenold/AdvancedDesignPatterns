Decorator Design Pattern

Task: 
Create a program that allows users to calculate the area of a rectangle. 
Implement the Decorator pattern to allow users to select different measurement units (e.g. inches, centimeters) 
and display the result in different formats (e.g. decimal, scientific notation). 

Use console output to display the calculated area.
